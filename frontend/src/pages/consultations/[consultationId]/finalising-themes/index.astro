---
import Layout from "../../../../layouts/Layout.astro";

import FinalisingThemeArchive from "../../../../components/screens/FinalisingThemeArchive/FinalisingThemeArchive.svelte";
import Section from "../../../../components/Section/Section.svelte";
import type { AstroGlobalRuntime } from "../../../../global/types";

const { consultationId } = (Astro as unknown as AstroGlobalRuntime).params;
---

<Layout>
  <Section>
    <FinalisingThemeArchive
      client:only="svelte"
      consultationId={consultationId || ""}
    />
  </Section>
</Layout>
