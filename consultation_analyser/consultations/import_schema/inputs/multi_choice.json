{
  "type": "object",
  "properties": {
    "themefinder_id": {
      "type": "integer",
      "description": "This is the ID for the respondent, provided by ThemeFinder."
    },
    "multi_choice_options": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Options that can be chosen for a closed question."
    }
  },
  "required": ["themefinder_id", "multi_choice_options"]
}
